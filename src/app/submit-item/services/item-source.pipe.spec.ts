import { ItemSourcePipe } from './item-source.pipe';

describe('ItemSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
